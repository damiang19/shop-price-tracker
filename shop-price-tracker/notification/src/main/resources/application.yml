server:
  port:9010

spring:
  application:
    name: notification
  cloud:
    function:
      definition: email|sms
  stream:
    bindings:
      emailsms-in-0:
        destiantion: send-communication
        group: ${spring.application.name}