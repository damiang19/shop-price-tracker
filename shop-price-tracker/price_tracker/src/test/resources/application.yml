spring:
  cloud:
    discovery:
      enabled: false
    config:
      enabled : false
  datasource:
    url: jdbc:tc:postgresql:11.1:///integration-tests-db
  jpa:
    hibernate:
      ddl-auto: create